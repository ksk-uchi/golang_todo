# golang_todo

## はじめに

1. `mise install`
2. `pnpm install`
3. `pnpm lefthook install`

## 開発環境の起動

**db**: `cd backend && podman compose up --build`
**backend**: `cd backend && air .`
**frontend**: `cd frontend && pnpm dev`
